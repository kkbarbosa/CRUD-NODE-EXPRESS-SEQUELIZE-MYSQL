<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css" type="text/css">
  <title>Editar Status da Leitura</title>
</head>
<body>
  <h2> Editar Status da Leitura: </h2>
  <h3> Livro: <%= livro.titulo %> </h3>
  <br>
  <form action="/editar-status/<%= leitura.id_livro %>" method="POST" class="formCadLivro">

    <% if (livro.imagem) { %>
      <img class="img" src="/uploads/<%= livro.imagem %>" width="200" height="300" alt="Imagem do Livro">
    <% } else { %>
      Sem imagem
    <% } %>

    <br><br>
    <label>Status da Leitura:</label>
    <select name="status_leitura" required class="botaoForm2">
      <option value="Não iniciado" <%= leitura.status_leitura === 'Não iniciado' ? 'selected' : '' %>>Não iniciado</option>
      <option value="Em andamento" <%= leitura.status_leitura === 'Em andamento' ? 'selected' : '' %>>Em andamento</option>
      <option value="Concluído" <%= leitura.status_leitura === 'Concluído' ? 'selected' : '' %>>Concluído</option>
    </select><br/><br/>

    <button class="botaoForm" type="submit">Atualizar Status</button>
    <br><br>
    <button class="botaoForm" ><a class="azin" href="/perfil">Voltar</a></button>
  </form>
</body>
<script src="/js/scriptAttStatus.js"></script>
</html>
